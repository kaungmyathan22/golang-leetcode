func findComplement(num int) int {
    // convert num to binary string
    binaryStr := (strconv.FormatInt(n, 2))
    // loop through it and reverse it
    for k,v := range binaryStr {
        fmt.Println(k,v)
    }
    // convert reverted str to num
    return 0
}

func isSameTree(p *TreeNode, q *TreeNode) bool {
    if q.Val != p.Val {
        return false
    }
    isLeftSame := isSameTree(p.Left, p.Left)
    isRightSame := isSameTree(p.Right, p.Right)
	return isLeftSame && isRightSame
}